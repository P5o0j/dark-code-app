import { IoIosCart } from "react-icons/io";
import Button from "../UI/Button";

function Cart() {
  return (
    <div>
      <Button>
        <IoIosCart /> (3)
      </Button>
    </div>
  );
}

export default Cart;
